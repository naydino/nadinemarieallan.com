---
---

<footer class="footer">
  <div class="footer-text">nadine marie allan · lisbon</div>
  <div class="footer-text">nadinemarieallan.com</div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--border);
    padding: var(--space-4) var(--page-padding-x);
    max-width: var(--site-max);
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    flex-wrap: wrap;
    gap: var(--space-1);
  }
  @media (max-width: 600px) {
    .footer { padding: var(--space-4) var(--page-padding-x-sm); }
  }
  .footer-text {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    font-weight: var(--w-regular);
    text-transform: lowercase;
    color: var(--muted);
  }
</style>
